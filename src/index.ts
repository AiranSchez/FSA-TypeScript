import { PersonalComputer } from './PersonalComputer';

let ordenador: PersonalComputer = {
    name: 'Predator',
    brand: 'Acer',
    price: 200
}

const sum = (num1: number, num2: number): number => {
    return num1 + num2;
}

export default sum
